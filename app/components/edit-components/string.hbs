<Criteria::SelectStringCriterion
  @value={{this.value}}
  @treeNode={{@node}}
  as |label check score|>
  {{#if label}}
    <AuCriteriaInline @criterium={{@node.title}} @score={{score}} @comment={{label}} />
  {{/if}}
</Criteria::SelectStringCriterion>