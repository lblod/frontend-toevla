<Criteria::SelectStringCriterion
  @value={{this.value}}
  @treeNode={{@node}}
  as |label check score|>
  {{#if label}}
    <Labels::NumericString @label={{label}} @value={{this.value}} as |string|>
      <AuCriteiraInline @criterium={{string}} @score={{score}} @comment={{@comment}} />
    </Labels::NumericString>
  {{/if}}
</Criteria::SelectStringCriterion>