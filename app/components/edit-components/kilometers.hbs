<Criteria::SelectCriterion
  @value={{this.kilometers}}
  @treeNode={{@node}}
  as |label check score|>
  {{#if label}}
    <Labels::NumericString @label={{label}} @value={{this.kilometers}} as |string|>
      <AuCriteriaInline @criterium={{string}} @score={{score}} @etns={{@etns}} />
    </Labels::NumericString>
  {{/if}}
</Criteria::SelectCriterion>