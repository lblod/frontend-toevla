<Criteria::SelectBooleanCriterion
  @value={{this.value}}
  @treeNode={{@node}}
  as |label check|>
  {{#if label}}
    <AuCriteriaInline @criterium={{@node.title}} @check={{check}}>
      <strong>{{label}}</strong>
    </AuCriteriaInline>
  {{/if}}
</Criteria::SelectBooleanCriterion>
