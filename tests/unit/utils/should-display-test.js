import shouldDisplay from 'frontend-toevla/utils/should-display';
import { module, test } from 'qunit';

module('Unit | Utility | should-display', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = shouldDisplay();
    assert.ok(result);
  });
});
